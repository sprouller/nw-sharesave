{
  "dependencies": {
    "@supabase/supabase-js": "^2.12.1",
    "serve": "^14.2.0"
  },
  "name": "nw-calc",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "serve",
    "build": "esbuild v1/index.js --bundle --minify --outfile=dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "esbuild": "^0.19.4"
  }
}
